204 表示使用PUT/Delete时，服务器成功处理，但是未返回任何内容 (no content)

206 客户端发进了范围请求，服务端成功执行了这部分GET请求

301 永久重定向  表示请求的资源被分配到了新的URI，以后应使用资源现在所指的URI

302 临时性重定向 请求的资源被分到了新的URI，希望用户本次使用新的URI。以后该资源以后可能还会使用新的URI

304 HTTP请求如果是if - modified - since ，返回304，意思是从给的那个时间点到现在，该资源未更新。由于客户端会缓存访问过的资源，这个时候客户端只需要读缓存即可。

400 请求报文中存在语法错误

401 未认证；表示发送的请求需要有通过HTTP认证的认证信息。

403 对资源的请求被服务器拒绝了

500 服务器内部故障 可能是web应用出bug或者某些临时故障

503 服务不可达 服务器处于超负载或者正在处于停机维护

